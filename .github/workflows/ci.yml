jobs:
  build:
    runs-on: ubuntu-latest

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v4.2.0

      - name: Build Docker
        run: docker build --no-cache -t cpp-hello .

      - name: Run test
        run: docker run --rm cpp-hello